## Convex bootstrap

Run `npx convex dev` after creating a Convex project and setting `VITE_CONVEX_URL`.

The schema file is included now because Block 1 explicitly requires the data model early. Actions and queries should be added incrementally as the media pipeline comes online.
